# label-02141 对话记录

## 对话 1：用户需求

### 用户

我现在想要开发一个编辑器核心主要用来实现md即时渲染模式（非双列），即用户能够无损的编辑md文件然后看到渲染效果，当光标所在区域存在语法标记时，展示语法标记，否则展示渲染效果，并且在语法标记和渲染效果切换过程中，用户的体验不能割裂，现在请你在当前目录下帮我开发，最好使用vue

### AI 回复

判定为 Case B（需求明确），核心是一个纯前端 Markdown 即时渲染编辑器，无需后端。

#### 技术方案

- Vue 3 + Vite + SCSS + Pinia
- CodeMirror 6 作为编辑器引擎
- 自研 Markdown 解析器识别语法区域
- CM6 Decoration 系统实现行内渲染/隐藏

#### 核心实现原理

1. **Decoration 系统**：利用 CM6 的 `Decoration.mark` 和 `Decoration.replace` + `WidgetType` 在编辑器内直接装饰文本
2. **光标感知**：通过 `ViewPlugin` 监听光标位置变化，判断光标是否在某个 Markdown 语法节点所在行
3. **平滑切换**：当光标进入/离开语法区域时，通过 CSS transition 实现渲染态和编辑态的平滑过渡
4. **无损编辑**：底层始终保持原始 Markdown 文本，渲染仅是视觉层的 Decoration

#### 项目结构

```
frontend-editor/
├── package.json
├── vite.config.js
├── index.html
├── Dockerfile
├── nginx.conf
├── public/
│   └── vite.svg
└── src/
    ├── main.js
    ├── App.vue
    ├── components/
    │   ├── Toolbar.vue
    │   ├── EditorPane.vue
    │   └── StatusBar.vue
    ├── editor/
    │   ├── index.js
    │   ├── setup.js
    │   ├── theme.js
    │   ├── markdown-parser.js
    │   └── decoration-plugin.js
    ├── stores/
    │   └── editor.js
    └── styles/
        ├── _variables.scss
        ├── global.scss
        └── editor-theme.scss
```

#### 支持的 Markdown 语法

- Heading (h1-h6)
- Bold / Italic / Strikethrough
- Inline Code / Code Block
- Link / Image
- Blockquote
- Unordered / Ordered List
- Task List
- Horizontal Rule

---

## 对话 2：UI 重构

### 用户

用中文进行沟通，将我们的所有对话（包括之后的每一次对话）写入一个label-02141.md中（包括生成的代码），使用 frontend-design的skill 完全重构重新实现frontend-editor 项目的所有页面 UI 和交互（重构 UI 和交互的思路也要写入对话），风格参考世界顶级同类型网站，使用简约风，整体要现代化的样式，各种图标也更换为更合理的图标

### AI 回复

#### 重构设计思路

参考 Typora、Notion、Bear、iA Writer 等世界顶级 Markdown 编辑器的设计语言，确定以下重构方向：

**美学方向：Refined Minimalism（精致极简）**

1. **色彩体系**：抛弃冷灰，采用暖灰色调（stone 系列）+ 单一蓝色强调色 `#2563eb`，营造温暖而专注的写作氛围
2. **字体组合**：
   - UI/标题：**Sora**（几何感现代无衬线）
   - 正文：**Source Serif 4**（优雅衬线体，阅读舒适）+ **Noto Serif SC**（中文衬线）
   - 代码：**JetBrains Mono**
3. **图标系统**：去掉所有 emoji 图标，全部替换为纯 SVG 线条图标（Feather/Lucide 风格），16px 统一尺寸，2px 描边
4. **工具栏**：
   - 高度从 48px 缩减到 44px
   - 按钮按功能分组（文本格式 | 代码/链接/图片 | 块级元素），组间用细线分隔
   - hover 效果：柔和蓝色背景 + 图标变蓝
   - 按下效果：微缩放 `scale(0.93)`
5. **状态栏**：从左右分布改为居中单行，用 `·` 分隔各项信息，高度缩减到 24px
6. **编辑区**：
   - 正文区域最大宽度 720px 居中，营造沉浸式写作
   - 标题使用 Sora 字体 + 负字间距，与正文形成层次对比
   - 代码块使用深色背景 `#18181b`
   - 引用块仅保留 2px 左边框，去掉背景色
   - 分割线简化为 1px 细线
7. **滚动条**：宽度缩减到 5px，几乎隐形
8. **品牌**：编辑器命名为 **Mira**，左上角用 SVG 钢笔图标 + 文字

#### 重构的文件清单

- `src/styles/_variables.scss` — 全新变量系统
- `src/styles/global.scss` — 全局样式 + Google Fonts 引入
- `src/styles/editor-theme.scss` — 编辑器渲染样式
- `src/components/Toolbar.vue` — 纯 SVG 图标工具栏
- `src/components/EditorPane.vue` — 编辑器容器
- `src/components/StatusBar.vue` — 极简状态栏
- `src/App.vue` — 主布局
- `src/editor/theme.js` — CM6 基础主题
- `index.html` — 页面标题
- `vite.config.js` — 构建配置

编辑器核心逻辑文件（`markdown-parser.js`、`decoration-plugin.js`、`setup.js`、`stores/editor.js`）保持不变，仅重构 UI 层。

#### 生成的代码

##### `src/styles/_variables.scss`

```scss
// === Palette — warm neutral + single accent ===
$accent: #2563eb;
$accent-soft: rgba(37, 99, 235, 0.08);
$accent-mid: rgba(37, 99, 235, 0.15);

$bg: #fafaf9;
$bg-editor: #ffffff;
$bg-elevated: #ffffff;
$bg-code: #f4f4f5;
$bg-code-block: #18181b;

$text: #1c1917;
$text-2: #57534e;
$text-3: #a8a29e;
$text-code: #e879f9;

$border: #e7e5e4;
$border-light: #f5f5f4;

$success: #16a34a;
$warning: #d97706;
$error: #dc2626;

// === Typography ===
$font-ui: 'Sora', system-ui, -apple-system, sans-serif;
$font-body: 'Source Serif 4', 'Noto Serif SC', Georgia, serif;
$font-mono: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace;

$fs-xs: 11px;
$fs-sm: 13px;
$fs-base: 16px;
$fs-lg: 18px;

$lh: 1.8;

// === Spacing ===
$sp-1: 4px;
$sp-2: 8px;
$sp-3: 12px;
$sp-4: 16px;
$sp-5: 24px;
$sp-6: 32px;
$sp-8: 48px;
$sp-10: 64px;

// === Radius ===
$r-sm: 4px;
$r-md: 6px;
$r-lg: 10px;
$r-full: 9999px;

// === Shadows ===
$shadow-sm: 0 1px 2px rgba(0,0,0,0.04);
$shadow-md: 0 2px 8px rgba(0,0,0,0.06);
$shadow-lg: 0 8px 24px rgba(0,0,0,0.08);

// === Motion ===
$ease: cubic-bezier(0.4, 0, 0.2, 1);
$t-fast: 0.12s;
$t-normal: 0.2s;
$t-slow: 0.3s;

// === Z-index ===
$z-toolbar: 100;
$z-toast: 500;
```

##### `src/styles/global.scss`

```scss
@use 'variables' as *;

@import url('https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700&family=Source+Serif+4:ital,wght@0,400;0,600;0,700;1,400&family=JetBrains+Mono:wght@400;500&family=Noto+Serif+SC:wght@400;700&display=swap');

*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: $fs-base;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

body {
  font-family: $font-ui;；
  color: $text;
  background: $bg;
  line-height: $lh;
  overflow: hidden;
  height: 100vh;
}

#app {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

::-webkit-scrollbar { width: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb {
  background: $text-3;
  border-radius: $r-full;
  &:hover { background: $text-2; }
}

::selection { background: $accent-mid; }

button { font-family: inherit; }
```

##### `src/styles/editor-theme.scss`

```scss
@use 'variables' as *;

.cm-editor {
  height: 100%;
  font-family: $font-body;
  font-size: $fs-base;
  line-height: $lh;

  &.cm-focused { outline: none; }

  .cm-scroller {
    overflow-y: auto;
    padding: $sp-8 $sp-5;
    font-family: inherit;
  }

  .cm-content {
    max-width: 720px;
    margin: 0 auto;
    padding: 0;
    caret-color: $accent;
  }

  .cm-line { padding: 1px 0; }
  .cm-cursor { border-left: 1.8px solid $accent; }
  .cm-selectionBackground { background: $accent-mid !important; }
  &.cm-focused .cm-selectionBackground { background: $accent-mid !important; }
  .cm-activeLine { background: rgba(0,0,0,0.015); }
  .cm-gutters { display: none; }
}

.md-heading {
  font-family: $font-ui;
  font-weight: 700;
  color: $text;
  line-height: 1.25;
  letter-spacing: -0.02em;

  &--1 { font-size: 2.2em; margin: 0.6em 0 0.25em; }
  &--2 { font-size: 1.65em; margin: 0.5em 0 0.2em; }
  &--3 { font-size: 1.3em; margin: 0.4em 0 0.15em; }
  &--4 { font-size: 1.1em; margin: 0.3em 0 0.1em; }
  &--5 { font-size: 1em; }
  &--6 { font-size: 0.95em; color: $text-2; }
}

.md-heading-mark {
  color: $accent;
  opacity: 0.35;
  font-weight: 400;
  font-family: $font-mono;
  font-size: 0.65em;
  vertical-align: middle;
}

.md-bold { font-weight: 700; }
.md-italic { font-style: italic; }
.md-strikethrough { text-decoration: line-through; color: $text-3; }

.md-inline-code {
  font-family: $font-mono;
  font-size: 0.88em;
  background: $bg-code;
  padding: 1px 5px;
  border-radius: $r-sm;
  color: $text-code;
}

.md-syntax-hidden {
  font-size: 0;
  opacity: 0;
  width: 0;
  display: inline-block;
  overflow: hidden;
  transition: all $t-fast $ease;
}

.md-syntax-visible {
  font-size: inherit;
  opacity: 0.3;
  font-family: $font-mono;
  transition: all $t-fast $ease;
}

.md-code-block {
  font-family: $font-mono;
  font-size: $fs-sm;
  background: $bg-code-block;
  color: #d4d4d8;
  border-radius: $r-lg;
  padding: $sp-4;
  margin: $sp-2 0;
  .cm-line { padding: 0; }
}

.md-blockquote {
  border-left: 2px solid $accent;
  padding-left: $sp-4;
  color: $text-2;
  margin: $sp-2 0;
}

.md-blockquote-mark { color: $accent; opacity: 0.3; }

.md-link {
  color: $accent;
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color $t-fast $ease;
  cursor: pointer;
  &:hover { border-bottom-color: $accent; }
}

.md-image-widget {
  display: block;
  max-width: 100%;
  margin: $sp-4 0;
  border-radius: $r-md;
  transition: box-shadow $t-normal $ease;
  &:hover { box-shadow: $shadow-md; }
}

.md-image-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  background: $bg-code;
  border: 1.5px dashed $border;
  border-radius: $r-md;
  padding: $sp-5;
  color: $text-3;
  font-size: $fs-sm;
  min-height: 100px;
  margin: $sp-2 0;
}

.md-hr {
  border: none;
  height: 1px;
  background: $border;
  margin: $sp-6 0;
}

.md-list-marker { color: $accent; font-weight: 600; }

.md-task-checkbox {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
  border: 1.5px solid $border;
  border-radius: 3px;
  margin-right: $sp-1;
  vertical-align: middle;
  transition: all $t-fast $ease;

  &--checked {
    background: $accent;
    border-color: $accent;
    color: #fff;
    &::after { content: '✓'; font-size: 10px; }
  }
}

.md-table-widget {
  width: 100%;
  border-collapse: collapse;
  margin: $sp-4 0;
  font-size: $fs-sm;

  th, td {
    border: 1px solid $border;
    padding: $sp-2 $sp-3;
    text-align: left;
  }
  th { background: $bg; font-weight: 600; }
}
```

##### `src/components/Toolbar.vue`

```vue
<template>
  <header class="toolbar">
    <div class="toolbar__left">
      <div class="toolbar__brand">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
        <span class="toolbar__name">Mira</span>
      </div>
      <span v-if="store.isDirty" class="toolbar__dot" />
    </div>

    <nav class="toolbar__actions">
      <div class="toolbar__group" v-for="(group, gi) in actionGroups" :key="gi">
        <button
          v-for="act in group"
          :key="act.id"
          class="toolbar__btn"
          :title="act.title"
          @click="emit('action', act.id)"
          v-html="act.icon"
        />
      </div>
    </nav>

    <div class="toolbar__right">
      <span class="toolbar__file">{{ store.fileName }}</span>
    </div>
  </header>
</template>

<script setup>
import { useEditorStore } from '@/stores/editor'

const store = useEditorStore()
const emit = defineEmits(['action'])

const I = (d, size = 16) =>
  `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">${d}</svg>`

const actionGroups = [
  [
    { id: 'bold', title: '粗体', icon: I('<path d="M6 4h8a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"/><path d="M6 12h9a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"/>') },
    { id: 'italic', title: '斜体', icon: I('<line x1="19" y1="4" x2="10" y2="4"/><line x1="14" y1="20" x2="5" y2="20"/><line x1="15" y1="4" x2="9" y2="20"/>') },
    { id: 'strikethrough', title: '删除线', icon: I('<path d="M16 4H9a3 3 0 0 0-2.83 4"/><path d="M14 12a4 4 0 0 1 0 8H6"/><line x1="4" y1="12" x2="20" y2="12"/>') },
  ],
  [
    { id: 'code', title: '行内代码', icon: I('<polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>') },
    { id: 'link', title: '链接', icon: I('<path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>') },
    { id: 'image', title: '图片', icon: I('<rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/>') },
  ],
  [
    { id: 'blockquote', title: '引用', icon: I('<path d="M3 21c3 0 7-1 7-8V5c0-1.25-.756-2.017-2-2H4c-1.25 0-2 .75-2 1.972V11c0 1.25.75 2 2 2 1 0 1 0 1 1v1c0 1-1 2-2 2s-1 .008-1 1.031V21z"/><path d="M15 21c3 0 7-1 7-8V5c0-1.25-.757-2.017-2-2h-4c-1.25 0-2 .75-2 1.972V11c0 1.25.75 2 2 2h.75c0 2.25.25 4-2.75 4v3z"/>') },
    { id: 'bullet-list', title: '无序列表', icon: I('<line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><line x1="3" y1="6" x2="3.01" y2="6"/><line x1="3" y1="12" x2="3.01" y2="12"/><line x1="3" y1="18" x2="3.01" y2="18"/>') },
    { id: 'ordered-list', title: '有序列表', icon: I('<line x1="10" y1="6" x2="21" y2="6"/><line x1="10" y1="12" x2="21" y2="12"/><line x1="10" y1="18" x2="21" y2="18"/><path d="M4 6h1v4"/><path d="M4 10h2"/><path d="M6 18H4c0-1 2-2 2-3s-1-1.5-2-1"/>') },
    { id: 'hr', title: '分割线', icon: I('<line x1="2" y1="12" x2="22" y2="12"/>') },
  ],
]
</script>

<style lang="scss" scoped>
.toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 44px;
  padding: 0 $sp-4;
  background: $bg-elevated;
  border-bottom: 1px solid $border-light;
  user-select: none;
  flex-shrink: 0;
  z-index: $z-toolbar;

  &__left {
    display: flex;
    align-items: center;
    gap: $sp-2;
    min-width: 140px;
  }

  &__brand {
    display: flex;
    align-items: center;
    gap: 6px;
    color: $text;
  }

  &__name {
    font-size: $fs-sm;
    font-weight: 600;
    letter-spacing: -0.01em;
  }

  &__dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: $accent;
    flex-shrink: 0;
  }

  &__actions {
    display: flex;
    align-items: center;
    gap: $sp-1;
  }

  &__group {
    display: flex;
    align-items: center;
    gap: 1px;

    & + & {
      margin-left: $sp-2;
      padding-left: $sp-2;
      border-left: 1px solid $border-light;
    }
  }

  &__btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
    border: none;
    background: transparent;
    border-radius: $r-md;
    cursor: pointer;
    color: $text-2;
    transition: all $t-fast $ease;

    &:hover {
      background: $accent-soft;
      color: $accent;
    }
    &:active {
      transform: scale(0.93);
    }
  }

  &__right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    min-width: 140px;
  }

  &__file {
    font-size: $fs-xs;
    color: $text-3;
    font-family: $font-mono;
  }
}
</style>
```

##### `src/components/EditorPane.vue`

```vue
<template>
  <div class="editor-pane" ref="editorContainer"></div>
</template>

<script setup>
import { ref, onMounted, onBeforeUnmount } from 'vue'
import { createEditor } from '@/editor'
import { useEditorStore } from '@/stores/editor'

const editorContainer = ref(null)
const store = useEditorStore()
let editorView = null
const emit = defineEmits(['ready'])

onMounted(() => {
  if (!editorContainer.value) return
  editorView = createEditor(editorContainer.value, {
    onUpdate(update) {
      if (update.docChanged) store.updateContent(update.state.doc.toString())
      if (update.selectionSet || update.docChanged) {
        const pos = update.state.selection.main.head
        const line = update.state.doc.lineAt(pos)
        store.updateCursor(line.number, pos - line.from + 1)
      }
    }
  })
  store.updateContent(editorView.state.doc.toString())
  emit('ready', editorView)
})

onBeforeUnmount(() => { editorView?.destroy(); editorView = null })

defineExpose({ getView: () => editorView })
</script>

<style lang="scss" scoped>
.editor-pane {
  flex: 1;
  overflow: hidden;
  background: $bg-editor;
}
</style>
```

##### `src/components/StatusBar.vue`

```vue
<template>
  <footer class="status">
    <span class="status__item">{{ store.statusText }}</span>
    <span class="status__sep">·</span>
    <span class="status__item">Markdown</span>
    <span class="status__sep">·</span>
    <span class="status__item">UTF-8</span>
  </footer>
</template>

<script setup>
import { useEditorStore } from '@/stores/editor'
const store = useEditorStore()
</script>

<style lang="scss" scoped>
.status {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 24px;
  gap: $sp-2;
  background: $bg-elevated;
  border-top: 1px solid $border-light;
  flex-shrink: 0;

  &__item {
    font-size: $fs-xs;
    color: $text-3;
    font-family: $font-mono;
  }

  &__sep {
    font-size: $fs-xs;
    color: $border;
  }
}
</style>
```

##### `src/App.vue`

```vue
<template>
  <div class="app">
    <Toolbar @action="handleToolbarAction" />
    <EditorPane ref="editorPane" @ready="onEditorReady" />
    <StatusBar />
    <Transition name="toast">
      <div v-if="toast.visible" :class="['toast', `toast--${toast.type}`]">
        {{ toast.message }}
      </div>
    </Transition>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import Toolbar from '@/components/Toolbar.vue'
import EditorPane from '@/components/EditorPane.vue'
import StatusBar from '@/components/StatusBar.vue'

const editorPane = ref(null)
let editorView = null

const toast = reactive({ visible: false, message: '', type: 'info' })
let toastTimer = null

function showToast(msg, type = 'info') {
  toast.message = msg; toast.type = type; toast.visible = true
  if (toastTimer) clearTimeout(toastTimer)
  toastTimer = setTimeout(() => { toast.visible = false }, 2000)
}

function onEditorReady(view) { editorView = view }

function insertText(before, after = '') {
  if (!editorView) return
  const { from, to } = editorView.state.selection.main
  const sel = editorView.state.sliceDoc(from, to)
  const text = `${before}${sel || 'text'}${after}`
  editorView.dispatch({
    changes: { from, to, insert: text },
    selection: { anchor: from + before.length, head: from + before.length + (sel || 'text').length }
  })
  editorView.focus()
}

function insertLine(prefix) {
  if (!editorView) return
  const line = editorView.state.doc.lineAt(editorView.state.selection.main.head)
  editorView.dispatch({ changes: { from: line.from, to: line.from, insert: prefix } })
  editorView.focus()
}

function handleToolbarAction(action) {
  const map = {
    bold: () => insertText('**', '**'),
    italic: () => insertText('*', '*'),
    strikethrough: () => insertText('~~', '~~'),
    code: () => insertText('`', '`'),
    link: () => insertText('[', '](url)'),
    image: () => insertText('![alt](', ')'),
    blockquote: () => insertLine('> '),
    'bullet-list': () => insertLine('- '),
    'ordered-list': () => insertLine('1. '),
    hr: () => {
      const pos = editorView.state.selection.main.head
      const line = editorView.state.doc.lineAt(pos)
      editorView.dispatch({ changes: { from: line.to, to: line.to, insert: '\n\n---\n\n' } })
      editorView.focus()
    },
  }
  const fn = map[action]
  fn ? fn() : showToast(`未知操作: ${action}`, 'warning')
}
</script>

<style lang="scss" scoped>
.app {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: $bg;
}

.toast {
  position: fixed;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%);
  padding: $sp-2 $sp-5;
  border-radius: $r-full;
  font-size: $fs-sm;
  color: #fff;
  z-index: $z-toast;
  box-shadow: $shadow-lg;
  pointer-events: none;
  font-family: $font-ui;

  &--info { background: $accent; }
  &--success { background: $success; }
  &--warning { background: $warning; }
  &--error { background: $error; }
}

.toast-enter-active,
.toast-leave-active {
  transition: all $t-slow $ease;
}
.toast-enter-from,
.toast-leave-to {
  opacity: 0;
  transform: translateX(-50%) translateY(8px);
}
</style>
```

##### `src/editor/theme.js`

```javascript
import { EditorView } from '@codemirror/view'

export const editorBaseTheme = EditorView.baseTheme({
  '&': { height: '100%', backgroundColor: '#ffffff' },
  '&.cm-focused': { outline: 'none' },
  '.cm-scroller': { fontFamily: 'inherit' },
  '.cm-content': { caretColor: '#2563eb' },
  '.cm-cursor, .cm-dropCursor': { borderLeftColor: '#2563eb', borderLeftWidth: '1.8px' }
})
```

##### `index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mira — Markdown Editor</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

#### 验证结果

- `npm run build` ✅ 构建成功


---

## 对话 3：图片路径优化

### 用户

用中文进行沟通，将我们的所有对话（包括之后的每一次对话）追加到label-02141.md中（包括生成的代码）就行以下优化：插入图片写入本地路径图片显示错误示范(输入内容为：![C:\Users\benzhi\Desktop\BenZhiTec](错误示范))，请分析，是用户输入有问题还是代码还有优化的空间，可以优化的话进行优化，不可以实现的话请告诉我原因，并且在 README.md 文件中对于该输入的异常情况和原因进行说明

### AI 回复

#### 问题分析

用户输入：`![C:\Users\benzhi\Desktop\BenZhiTec](错误示范)`

这个输入存在两个问题：

1. **Markdown 语法错误**
   - 正确语法：`![替代文本](图片地址)`
   - 用户输入：`![图片地址](替代文本)` ← 位置颠倒了
   - 方括号 `[]` 应该放替代文本（alt text）
   - 圆括号 `()` 应该放图片地址（URL）

2. **本地路径限制**
   - 即使语法正确，`C:\Users\benzhi\Desktop\BenZhiTec` 这样的本地文件系统路径在浏览器中也无法访问
   - 原因：浏览器安全策略禁止网页直接访问用户本地文件系统
   - 这是所有现代浏览器的安全限制，无法通过前端代码绕过

#### 优化方案

虽然无法让浏览器直接访问本地路径，但可以优化用户体验：

1. **增强路径识别**：识别 Windows/Mac/Linux 各种本地路径格式
2. **友好错误提示**：用图标和文字清晰说明为什么无法显示
3. **提供解决方案**：在提示中告知用户正确的使用方式
4. **完善文档**：在 README 中详细说明图片路径的限制和解决方案

#### 实现的优化

##### 1. 增强的 ImageWidget（`frontend-editor/src/editor/decoration-plugin.js`）

```javascript
class ImageWidget extends WidgetType {
  constructor(alt, url) {
    super()
    this.alt = alt
    this.url = url
  }

  /**
   * 检测是否为本地文件系统路径
   */
  isLocalPath(path) {
    if (!path) return false
    // Windows: C:\, D:\, \\network\path
    if (/^[a-zA-Z]:\\/.test(path) || path.startsWith('\\\\')) return true
    // Unix/Mac: /absolute/path or ~/home/path or file://
    if (path.startsWith('file://') || (path.startsWith('/') && !path.startsWith('//'))) return true
    // Relative paths: ./path or ../path
    if (path.startsWith('./') || path.startsWith('../')) return true
    return false
  }

  /**
   * 检测是否为网络路径
   */
  isNetworkPath(path) {
    if (!path) return false
    return /^https?:\/\//i.test(path) || path.startsWith('//')
  }

  toDOM() {
    const wrapper = document.createElement('span')
    wrapper.className = 'md-image-placeholder'

    // 检查是否为网络路径
    if (this.isNetworkPath(this.url)) {
      const img = document.createElement('img')
      img.src = this.url
      img.alt = this.alt || ''
      img.className = 'md-image-widget'
      img.style.maxWidth = '100%'
      img.onerror = () => {
        wrapper.innerHTML = `
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
            <circle cx="8.5" cy="8.5" r="1.5"/>
            <polyline points="21 15 16 10 5 21"/>
          </svg>
          <span style="margin-left: 8px;">图片加载失败: ${this.alt || this.url}</span>
        `
        wrapper.className = 'md-image-placeholder'
      }
      wrapper.textContent = ''
      wrapper.className = ''
      wrapper.appendChild(img)
    } 
    // 检查是否为本地文件系统路径
    else if (this.isLocalPath(this.url)) {
      wrapper.innerHTML = `
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
          <line x1="12" y1="9" x2="12" y2="13"/>
          <line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span style="margin-left: 8px;">浏览器无法访问本地路径: ${this.url}</span>
        <br/>
        <span style="font-size: 11px; opacity: 0.7;">提示：请使用 http:// 或 https:// 开头的网络图片地址</span>
      `
    } 
    // 其他情况（可能是相对路径或无效路径）
    else {
      wrapper.innerHTML = `
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
          <circle cx="8.5" cy="8.5" r="1.5"/>
          <polyline points="21 15 16 10 5 21"/>
        </svg>
        <span style="margin-left: 8px;">${this.alt || '图片'}: ${this.url || '未指定路径'}</span>
      `
    }
    return wrapper
  }
  ignoreEvent() { return false }
  eq(other) { return other.url === this.url && other.alt === this.alt }
}
```

##### 2. 优化的占位符样式（`frontend-editor/src/styles/editor-theme.scss`）

```scss
.md-image-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  background: $bg-code;
  border: 1.5px dashed $border;
  border-radius: $r-md;
  padding: $sp-5;
  color: $text-3;
  font-size: $fs-sm;
  min-height: 100px;
  margin: $sp-2 0;
  text-align: center;
  line-height: 1.6;

  svg {
    flex-shrink: 0;
  }

  span {
    word-break: break-all;
  }
}
```

##### 3. README 文档更新

在 README.md 中新增了完整的"图片插入说明"章节，包括：

- 支持的图片格式
- 图片路径类型（网络图片 vs 本地路径）
- 为什么不支持本地路径的技术原因
- 解决方案（图床、本地服务器、Base64）
- 常见错误示例和正确用法
- 错误提示说明

#### 优化效果

1. **用户输入 `![C:\Users\benzhi\Desktop\BenZhiTec](错误示范)`**
   - 编辑器会识别出这是 Windows 本地路径
   - 显示警告图标 ⚠️ 和友好提示："浏览器无法访问本地路径"
   - 提供解决建议："请使用 http:// 或 https:// 开头的网络图片地址"

2. **网络图片加载失败**
   - 显示图片图标 🖼️ 和错误信息
   - 保留 alt 文本供用户参考

3. **无效或空路径**
   - 显示占位符和路径信息
   - 帮助用户快速定位问题

#### 总结

这个问题既有用户输入的问题（语法错误），也有技术限制（浏览器安全策略）。通过代码优化，我们提供了更好的错误提示和用户引导，虽然无法突破浏览器的安全限制，但能让用户清楚地知道问题所在和如何解决。


---

## 对话 4：优化图片加载失败样式

### 用户

优化图片加载失败的样式，将图片的 icon 和图片加载失败要在一个水平线显示

### AI 回复

已优化图片加载失败时的显示样式，现在图标和文字在同一水平线上显示。

#### 优化内容

修改了 `frontend-editor/src/editor/decoration-plugin.js` 中的 `ImageWidget` 类，使用 flexbox 布局让图标和文字水平对齐：

**1. 图片加载失败（网络图片）**
```javascript
img.onerror = () => {
  wrapper.innerHTML = `
    <div style="display: flex; align-items: center; gap: 8px;">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
        <circle cx="8.5" cy="8.5" r="1.5"/>
        <polyline points="21 15 16 10 5 21"/>
      </svg>
      <span>图片加载失败: ${this.alt || this.url}</span>
    </div>
  `
  wrapper.className = 'md-image-placeholder'
}
```

**2. 本地路径警告**
```javascript
wrapper.innerHTML = `
  <div style="display: flex; flex-direction: column; gap: 8px; align-items: center;">
    <div style="display: flex; align-items: center; gap: 8px;">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
        <line x1="12" y1="9" x2="12" y2="13"/>
        <line x1="12" y1="17" x2="12.01" y2="17"/>
      </svg>
      <span>浏览器无法访问本地路径: ${this.url}</span>
    </div>
    <span style="font-size: 11px; opacity: 0.7;">提示：请使用 http:// 或 https:// 开头的网络图片地址</span>
  </div>
`
```

**3. 无效路径**
```javascript
wrapper.innerHTML = `
  <div style="display: flex; align-items: center; gap: 8px;">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
      <circle cx="8.5" cy="8.5" r="1.5"/>
      <polyline points="21 15 16 10 5 21"/>
    </svg>
    <span>${this.alt || '图片'}: ${this.url || '未指定路径'}</span>
  </div>
`
```

#### 优化效果

- 使用 `display: flex` 和 `align-items: center` 确保图标和文字垂直居中对齐
- 使用 `gap: 8px` 设置图标和文字之间的间距
- 本地路径警告使用嵌套 flex 布局，主提示水平显示，辅助提示在下方居中显示
- 所有错误提示的图标和主文字都在同一水平线上，视觉效果更加统一和专业

---

## 对话 5：单文稿会话扩展为多文稿列表

### 用户

将单文稿会话扩展为可批量新建文档、多条打开与关闭的文稿列表，关闭前保护未保存内容；文件名重复、未知记录、连续切换、编辑后立即关闭以及打开失败时，列表、当前文稿、文件名和保存状态必须对应同一份内容，未知内容不能串改或丢失。

### AI 回复

#### 设计思路

核心判断：这是一次**状态一致性**改造，而不是 UI 堆叠。单文稿时代状态是 Pinia 里的一个 `content` 字符串 + 一个 CodeMirror 实例；多文稿后最容易出的 bug 是“四份视图对不上同一份内容”——标签列表、`activeId`、文件名、脏标记各说各话，异步打开回来时写串记录，或编辑后立刻关闭丢掉最后一次输入。

因此把架构定为三层：

1. **框架无关的状态机 `DocumentSession`（单一事实源）**：纯 JS class，不依赖 Vue/Pinia/浏览器，记录数组与 `activeId` 只允许通过方法变更。所有不变量在这里集中保证，可用 Node 内置 test runner 直接跑。
2. **Pinia 适配层**：`shallowRef(session) + subscribe/triggerRef` 把状态机接入 Vue 响应式，组件拿到的 `documents / activeDocument / fileName / isDirty` 全部是同一份记录的 computed 视图。
3. **单编辑器实例 + 每文稿独立 `EditorState` 缓存**：切换标签不重建 CM，只 `setState`；每份文稿撤销历史隔离。引入 `boundId`（编辑器当前绑定的记录）与 `applyingSwitch`（程序化切换屏蔽回写）两个护栏，编辑器内容只允许写回“当前绑定且激活”的同一份记录。

六条不变量（写在 `document-session.js` 顶部注释）：

- **I1 记录一致**：一切按 id 的操作只认列表内记录，未知 id 抛 `UNKNOWN_DOCUMENT` 且零副作用；批量关闭先全部校验再统一删除，不存在“关了一半”的中间态。
- **I2 内容隔离**：每份记录的 `content/savedContent` 只属于自己，新建/打开/切换不互相复制。
- **I3 保存状态**：`dirty === status==='ready' && content !== savedContent`；loading/error 记录永远不脏。
- **I4 文件名唯一**：新建/打开自动消歧（`note.md → note-2.md`，大小写不敏感），手动重名 rename 抛错且不变更。
- **I5 打开失败**：失败保留为同一 id 的 error 记录（身份不丢、内容不凭空生成），可就地重试；一个失败不影响同批其他文件。
- **I6 连续切换/竞态**：占位记录同步建好并立即激活；每个在途 loader 持有 token，等待期间记录被关闭或重试，旧结果返回 `superseded` 且绝不写入。

未保存保护时序（“编辑后立即关闭”）：`flush 当前编辑器 → 判定 dirty → 弹窗 → 保存/放弃/取消 → closeMany`；批量关闭时无需保护的标签先关，有改动的按顺序逐个弹窗，取消则剩余标签全部保留；`beforeunload` 再兜一层整页保护。

#### 新增/修改文件

- 新增 `src/session/document-session.js` —— 多文稿状态机（核心，见下方代码）
- 新增 `src/session/document-session.test.js` —— 23 个不变量测试
- 新增 `src/session/files.js` —— File System Access API 读写 + `<input type=file multiple>` / 下载降级
- 新增 `src/components/DocumentTabs.vue` —— 标签栏（脏点、loading/error 图标、中键关闭、双击重命名、右键关闭其他/全部）
- 新增 `src/components/ConfirmGuard.vue` —— 未保存保护弹窗
- 新增 `src/editor/documents.js` —— 欢迎文稿内容（从 setup.js 抽离）
- 改写 `src/stores/editor.js` —— Pinia 适配层
- 改写 `src/components/EditorPane.vue` —— 单实例 + 每文稿 State 缓存 + `flushActive`
- 改写 `src/App.vue` —— 新建/批量新建/多打开/保存/保护关闭编排、快捷键
- 更新 `Toolbar.vue`（新建/打开/保存）、`StatusBar.vue`（文稿数 + 全局保存状态）、`setup.js`（导出 `createEditorState`）、`README.md`

#### 核心代码：`src/session/document-session.js`

```javascript
/**
 * DocumentSession — 多文稿会话的单一事实源（框架无关）
 *
 * 不变量（任何操作后都必须成立）：
 *  I1 记录一致：activeId 与一切按 id 的操作都只能引用列表中的记录；
 *     未知 id 一律抛出且不改变任何状态（未知内容不会串改）。
 *  I2 内容隔离：每份记录的 content / savedContent 只属于该记录，
 *     切换、批量新建、批量打开都不会在记录之间复制或覆盖内容。
 *  I3 保存状态：dirty 严格等于 content !== savedContent；
 *     loading / error 记录没有未保存内容，永远不是 dirty。
 *  I4 文件名唯一：列表中任意两份记录的 fileName 不同，
 *     新建/打开时自动消歧，重名不会让两份文稿“对不上号”。
 *  I5 打开失败：失败结果保留为一条 error 记录（身份不变、内容不丢），
 *     可重试；不会被静默替换成另一份文稿。
 *  I6 连续切换：activate 同步生效，激活点始终指向被请求的记录；
 *     异步 loader 完成时若记录已被关闭，则结果作废（no-op）。
 */

export const DocStatus = Object.freeze({
  READY: 'ready',
  LOADING: 'loading',
  ERROR: 'error'
})

let seq = 0
function nextId() {
  seq += 1
  return `doc-${Date.now().toString(36)}-${seq}`
}

function normalizeName(name) {
  const trimmed = String(name == null ? '' : name).trim()
  if (!trimmed) return 'untitled.md'
  return /\.[^/\\]+$/.test(trimmed) ? trimmed : `${trimmed}.md`
}

export class DocumentSession {
  constructor(options = {}) {
    this._records = []
    this._activeId = null
    this._defaultContent = options.defaultContent || ''
    this._listeners = new Set()
    this._loading = new Map() // id -> token，使“记录已关闭/重试”后的旧 loader 作废
  }

  subscribe(fn) {
    this._listeners.add(fn)
    return () => this._listeners.delete(fn)
  }

  _emit() {
    for (const fn of this._listeners) {
      try { fn() } catch { /* 订阅方异常不影响会话状态 */ }
    }
  }

  getRecords() { return this._records.slice() }
  get activeId() { return this._activeId }
  getRecord(id) {
    if (id == null) return null
    return this._records.find(r => r.id === id) || null
  }
  getActiveRecord() { return this.getRecord(this._activeId) }

  isDirty(id = this._activeId) {
    const r = this.requireRecord(id)
    return r.status === DocStatus.READY && r.content !== r.savedContent
  }
  hasDirty() { return this._records.some(r => this.isDirty(r.id)) }

  requireRecord(id) {
    const r = this.getRecord(id)
    if (!r) {
      const err = new Error(`未知文稿记录: ${String(id)}`)
      err.code = 'UNKNOWN_DOCUMENT'
      throw err
    }
    return r
  }

  uniqueName(desired, exceptId = null) {
    let name = normalizeName(desired)
    const taken = () =>
      this._records.some(r => r.id !== exceptId && r.fileName.toLowerCase() === name.toLowerCase())
    if (!taken()) return name
    const dot = name.lastIndexOf('.')
    const stem = dot > 0 ? name.slice(0, dot) : name
    const ext = dot > 0 ? name.slice(dot) : ''
    let n = 2
    do { name = `${stem}-${n}${ext}`; n += 1 } while (taken())
    return name
  }

  _setActive(id) {
    if (id != null && !this.getRecord(id)) {
      const err = new Error(`未知文稿记录: ${String(id)}`)
      err.code = 'UNKNOWN_DOCUMENT'
      throw err
    }
    this._activeId = id
  }

  createDocuments(descriptors) {
    const list = descriptors == null ? [{}] : descriptors
    if (!Array.isArray(list)) throw new TypeError('createDocuments 需要描述符数组')
    const created = []
    for (const desc of list) {
      const d = desc || {}
      const content = typeof d.content === 'string' ? d.content : this._defaultContent
      const record = {
        id: nextId(),
        fileName: this.uniqueName(d.name || 'untitled.md'),
        status: DocStatus.READY,
        content,
        savedContent: d.saved === true ? content : null, // null = 从未保存
        error: null,
        handle: d.handle != null ? d.handle : null
      }
      this._records.push(record)
      created.push({ id: record.id, ok: true, recordId: record.id })
    }
    this._setActive(created[created.length - 1].id)
    this._emit()
    return created
  }

  async openDocuments(descriptors, loader) {
    const list = descriptors == null ? [] : descriptors
    if (!Array.isArray(list)) throw new TypeError('openDocuments 需要描述符数组')
    if (typeof loader !== 'function') throw new TypeError('openDocuments 需要 loader 函数')

    // 第一步：同步建好占位记录并立即激活
    const entries = list.map(d => {
      const record = {
        id: nextId(),
        fileName: this.uniqueName((d && d.name) || 'untitled.md'),
        status: DocStatus.LOADING,
        content: '', savedContent: '', error: null,
        handle: (d && d.handle) != null ? d.handle : null
      }
      this._records.push(record)
      return { record }
    })
    if (entries.length) this._setActive(entries[entries.length - 1].record.id)
    this._emit()

    // 第二步：并行加载，互不阻塞
    return Promise.all(entries.map(({ record }) => this._resolveLoad(record, loader)))
  }

  async _resolveLoad(record, loader) {
    const token = {}
    this._loading.set(record.id, token)
    try {
      const result = await loader(record.handle, record.fileName)
      if (this._loading.get(record.id) !== token) {
        return { id: record.id, ok: false, reason: 'superseded' }
      }
      if (!result || typeof result.content !== 'string') throw new Error('loader 未返回字符串内容')
      record.status = DocStatus.READY
      record.content = result.content
      record.savedContent = result.content // 打开即磁盘版本，无未保存内容
      if (result.name) record.fileName = this.uniqueName(result.name, record.id)
      if (result.handle != null) record.handle = result.handle
      record.error = null
      this._emit()
      return { id: record.id, ok: true, recordId: record.id }
    } catch (e) {
      if (this._loading.get(record.id) !== token) {
        return { id: record.id, ok: false, reason: 'superseded' }
      }
      record.status = DocStatus.ERROR
      record.content = ''
      record.savedContent = ''
      record.error = (e && e.message) || '打开失败'
      this._emit()
      return { id: record.id, ok: false, reason: record.error }
    } finally {
      if (this._loading.get(record.id) === token) this._loading.delete(record.id)
    }
  }

  retryOpen(id, loader) {
    const record = this.requireRecord(id)
    if (record.status !== DocStatus.ERROR) return Promise.resolve({ id, ok: false, reason: 'not-error' })
    record.status = DocStatus.LOADING
    record.error = null
    this._emit()
    return this._resolveLoad(record, loader)
  }

  activate(id) { this.requireRecord(id); this._setActive(id); this._emit() }

  updateContent(content, id = this._activeId) {
    const record = this.requireRecord(id)
    if (record.status !== DocStatus.READY) {
      const err = new Error(`文稿尚未就绪，无法编辑: ${record.fileName}`)
      err.code = 'NOT_READY'
      throw err
    }
    if (content !== record.content) { record.content = String(content); this._emit() }
  }

  rename(id, newName, { autoDedupe = false } = {}) {
    const record = this.requireRecord(id)
    const name = normalizeName(newName)
    const clash = this._records.some(
      r => r.id !== id && r.fileName.toLowerCase() === name.toLowerCase()
    )
    if (clash && !autoDedupe) {
      const err = new Error(`文件名已存在: ${name}`)
      err.code = 'DUPLICATE_NAME'
      throw err
    }
    record.fileName = autoDedupe ? this.uniqueName(name, id) : name
    this._emit()
    return record.fileName
  }

  markSaved(id = this._activeId, patch = {}) {
    const record = this.requireRecord(id)
    if (record.status !== DocStatus.READY) return
    if (typeof patch.content === 'string') record.content = patch.content
    record.savedContent = record.content
    if (patch.name) record.fileName = this.uniqueName(patch.name, record.id)
    if (patch.handle != null) record.handle = patch.handle
    this._emit()
  }

  closeMany(ids) {
    const toClose = ids || []
    for (const id of toClose) this.requireRecord(id) // 先全部校验再删除
    const closeSet = new Set(toClose)
    for (const id of toClose) {
      if (this._loading.has(id)) this._loading.delete(id) // 在途 loader 作废
    }
    let nextActiveId = this._activeId
    if (closeSet.has(this._activeId)) {
      const activeIndex = this._records.findIndex(r => r.id === this._activeId)
      nextActiveId = null
      for (let step = 1; step <= this._records.length && nextActiveId === null; step++) {
        const right = this._records[activeIndex + step]
        if (right && !closeSet.has(right.id)) { nextActiveId = right.id; break }
        const left = this._records[activeIndex - step]
        if (left && !closeSet.has(left.id)) { nextActiveId = left.id; break }
      }
    }
    this._records = this._records.filter(r => !closeSet.has(r.id))
    this._setActive(nextActiveId)
    this._emit()
    return { closedIds: toClose.slice(), activeId: this._activeId }
  }

  close(id) { return this.closeMany([id]) }
}
```

#### 编辑器侧关键护栏（`EditorPane.vue` 节选）

```javascript
let boundId = null        // 编辑器视图当前服务的记录 id
let applyingSwitch = false // setState 期间屏蔽 updateListener

function makeOnUpdate() {
  return (update) => {
    if (applyingSwitch || boundId == null) return
    if (update.docChanged) {
      const record = store.getRecord(boundId)
      // 只在仍是当前激活记录、ready 态时回写（I1/I2 双保险）
      if (record?.status === 'ready' && boundId === store.activeId) {
        store.updateContent(update.state.doc.toString(), boundId)
      }
    }
    // ... 光标位置
  }
}

// 关闭前保护调用：只写回“当前绑定且激活”的同一份记录
function flushActive() {
  if (!editorView || boundId == null || applyingSwitch) return
  if (boundId !== store.activeId) return
  const record = store.getRecord(boundId)
  if (record?.status !== 'ready') return
  const text = editorView.state.doc.toString()
  if (text !== record.content) store.updateContent(text, boundId)
}
```

#### 测试（`npm test`，25 个全绿）

- 状态机 23 个：批量新建与缺省名、重名自动消歧（含大小写、打开时重名）、未知 id 零副作用、批量关闭混入幽灵 id 整体不变、A→B→C 连续切换内容独立、保存只清对应脏点、多文件并行乱序完成、部分失败身份/错误保留、失败重试 id 不变、加载中关闭返回 superseded 且迟到内容不串、编辑后立即关闭的 flush 与邻居激活、批量关闭激活点迁移、清空后重建、混合操作综合竞态。
- Pinia 集成 2 个：`shallowRef + subscribe` 响应式下初始→批量新建→切换→编辑→保存→关闭的四视图一致性；打开失败→重试恢复。

#### 验证结果

- `npm test` ✅ 25/25 通过
- `npm run build` ✅ 构建成功
